## CodeGrip Guidance

Use the local `.codegrip/` files as repository guidance when generating or changing code.

- Keep changes focused on the requested task.
- Follow existing project conventions.
- Preserve unrelated user edits.
- Suggest tests or checks for risky changes.
- Pay extra attention to auth, permissions, payments, migrations, deployment, config, secrets, and shared utilities.
